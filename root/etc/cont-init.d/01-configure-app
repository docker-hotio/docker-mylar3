#!/usr/bin/with-contenv bash
# shellcheck shell=bash

umask "${UMASK}"

if [[ ! -f "${CONFIG_DIR}/config.ini" ]]; then
    echo "[Git]" > "${CONFIG_DIR}/config.ini"
    echo "git_branch = python3-dev" >> "${CONFIG_DIR}/config.ini"
    chown hotio:hotio "${CONFIG_DIR}/config.ini"
fi
